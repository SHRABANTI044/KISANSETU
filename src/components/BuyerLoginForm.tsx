import RoleLoginForm from "./RoleLoginForm";

/** Buyer login form (blue theme) — shown after the Buyer role card. */
export default function BuyerLoginForm() {
  return <RoleLoginForm role="buyer" />;
}
