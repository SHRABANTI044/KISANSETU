import RoleLoginForm from "./RoleLoginForm";

/** Farmer login form (green theme) — shown after the Farmer role card. */
export default function FarmerLoginForm() {
  return <RoleLoginForm role="farmer" />;
}
